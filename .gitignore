node_modules/
dist/
*.tsbuildinfo
.env
.env.*
!.env.example
.idea/
.vscode/
.DS_Store
.claude/
temp/

# Private strategy packages — developed out-of-tree and installed as local
# plugins, NEVER committed here. Ignored so an
# accidental copy under packages/ can't be staged.
packages/*/private-*/

# Local-only: deployment config and internal design notes carry
# machine-specific paths; typecheck build output is derived.
/scripts/deploy.env
# The personal deploy script: deploy.sh is the generic one the repo ships,
# this is the one with a real host, key path and plugin list in it.
/scripts/deploy.local.sh
/docs/architecture.md
.next-typecheck/
