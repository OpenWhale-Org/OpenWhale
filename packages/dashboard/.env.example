# OpenWhale Dashboard environment variables
# Copy to .env.local and fill in values

# Master key for credential encryption (required)
OPENWHALE_MASTER_KEY=change-me-in-production

# Path to SQLite database file (optional, defaults to ~/.openwhale/openwhale.db)
OPENWHALE_DB_PATH=
