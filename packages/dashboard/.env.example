# OpenWhale Dashboard — pure frontend, holds NO secrets.
# Secrets (OPENWHALE_MASTER_KEY, LLM keys) belong to the gateway: repo-root .env or packages/gateway/.env.

# Where to reach the gateway (optional, default http://localhost:3001)
#OPENWHALE_GATEWAY_URL=http://localhost:3001
